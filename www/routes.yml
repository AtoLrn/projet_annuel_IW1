/:
    controller: main
    action: home

/contact:
    controller: main
    action: contact

/logout:
    controller: user
    action: logout
    security:
        - user
        - admin

/register-login:
    controller: user
    action: register_login

/mail-validation:
  controller: user
  action: mailValidation

/mail-validation:
  controller: user
  action: mailValidation

/forget:
    controller: user
    action: pwdforget

/handle-article:
    controller: article
    action: handleArticle

/recette:
    controller: article
    action: getArticle

/dashboard:
  controller: admin
  action: dashboard
  security:
    - admin
    - user

/list:
  controller: admin
  action: list
  security:


/get-user:
  controller: user
  action: getUserById
  security:


/get-users:
  controller: user
  action: getUsers
  security:
