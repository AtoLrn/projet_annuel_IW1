/:
  controller: main
  action: home

/contact:
  controller: main
  action: contact

/login:
  controller: user
  action: login

/logout:
  controller: user
  action: logout

/register:
  controller: user
  action: register

/mail-validation:
  controller: user
  action: mailValidation

/forget:
  controller: user
  action: pwdforget

/handle-article:
  controller: article
  action: handleArticle


/dashboard:
  controller: admin
  action: dashboard
  security:
    - admin
    - user